import {expect} from 'chai';
import {mount} from 'enzyme';
import React from 'react';
import App from '../../src/ui/App';
import {AppPage} from 'test/pages/AppPage';

describe('UI: e2e', () => {
  before(() => {
    
  });

  it('Go to host and client page', () => {

  });
});